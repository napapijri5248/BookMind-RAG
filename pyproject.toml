[project]
name = "rag-project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "napapijri5248", email = "jeetmandal5248@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "arxiv>=2.1.3,<3.0.0",
]

[project.scripts]
rag-project = "rag_project:main"

[build-system]
requires = ["uv_build>=0.12.13,<0.13.0"]
build-backend = "uv_build"
